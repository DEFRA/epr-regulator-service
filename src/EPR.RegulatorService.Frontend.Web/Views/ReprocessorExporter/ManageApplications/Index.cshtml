@using EPR.RegulatorService.Frontend.Core.Enums

@model EPR.RegulatorService.Frontend.Web.ViewModels.ReprocessorExporter.ManageApplicationsViewModel

@{
	ViewData["Title"] = $"{Model.Title} - {Model.ApplicationOrganisationType}";
}

<div class="govuk-width-container">

	<main class="govuk-main-wrapper govuk-!-padding-top-4" id="main-content" role="main">

		<div class="govuk-grid-row">
			<div class="govuk-grid-column-two-thirds govuk-!-margin-top-2 reprocessorExporter">
        <h1 class="govuk-heading-l">
          @Model.Title
        </h1>

		    @await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/_OrganisationDetails.cshtml", Model)


            @if (Model.ApplicationType == ApplicationType.Registration)
            {
              @await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/Registration/_CheckRegistrations.cshtml", Model)
              @await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/Registration/_ReviewPlasticRegistrations.cshtml", Model)
            } 


        @* @if (Model.ApplicationOrganisationType == ApplicationOrganisationType.Reprocessor)
		    {
		      @await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/_ReviewSiteDetails.cshtml", Model)
		    } *@
        
        @* @if (Model.ApplicationOrganisationType == ApplicationOrganisationType.Exporter)
        {
          @await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/_ReviewAddressAndPermits.cshtml", Model)
        } *@

      </div>
    </div>
  </main>
</div>
