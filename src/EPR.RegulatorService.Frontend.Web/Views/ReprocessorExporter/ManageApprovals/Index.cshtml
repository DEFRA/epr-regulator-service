@using EPR.RegulatorService.Frontend.Core.Enums
@model EPR.RegulatorService.Frontend.Web.ViewModels.ManageApprovalsViewModel

@{
	ViewData["Title"] = $"{Model.Title} - {Model.ApprovalOrganisationType}";
}

<div class="govuk-width-container">

	<main class="govuk-main-wrapper govuk-!-padding-top-4" id="main-content" role="main">

		<div class="govuk-grid-row">
			<div class="govuk-grid-column-two-thirds govuk-!-margin-top-2">
        <h1 class="govuk-heading-l">
          @Model.Title
        </h1>

		@await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/_OrganisationDetails.cshtml", Model)

        @* @if (Model.ApprovalType == ApprovalType.Registration)
        {
		      @await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/Registration/_CheckRegistrations.cshtml", Model)
        }

        @if (Model.ApprovalOrganisationType == ApprovalOrganisationType.Reprocessor)
		    {
		      @await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/_ReviewSiteDetails.cshtml", Model)
		    }
        
        @if (Model.ApprovalOrganisationType == ApprovalOrganisationType.Exporter)
        {
          @await Html.PartialAsync("~/Views/Shared/Partials/ReprocessorExporter/_ReviewAddressAndPermits.cshtml", Model)
        } *@

      </div>
    </div>
  </main>
</div>
